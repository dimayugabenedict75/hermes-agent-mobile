# Lucy Mobile — MEMORY

Compact memory rules for the Android client.

- Store identity profile locally: bot name, user name, backend URL
- Persist chat sessions in Room
- Do not attempt on-device inference
- Do not sync memory to PC automatically without explicit opt-in
- When in doubt, stay silent until user input exits silent mode
